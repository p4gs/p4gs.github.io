{
  "name": "sscsb-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run src/build.ts",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit",
    "refresh:controls": "bun run tools/control-ids.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.6.0"
  }
}
